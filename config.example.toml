[general]
# default_view = "today"       # TUI default view: today, upcoming, all, done
# theme = "omarchy"            # omarchy, dark, light, or custom theme name
# default_backend = "local"    # default backend for quick-add: local, linear

[waybar]
# tooltip_scope = "overdue_today"  # overdue_today, all, today_only

[backends.local]
enabled = true
# path = "~/.dewey/todo.txt"  # default location

# [backends.linear]
# enabled = true
# api_key = "lin_api_..."              # get one at https://linear.app/settings/api
# team_id = "..."                      # filled in by setup wizard
# team_name = "Engineering"
# assignee = "me"                      # "me" or a specific user ID
# user_id = "..."                      # filled in by setup wizard
# filter_status = ["In Progress", "Todo", "Backlog"]
